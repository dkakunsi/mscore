trigger:
  branches:
    exclude:
      - master
  paths:
    include:
      - mscore/history/**
    exclude: 
      - mscore/history/README.MD
      - mscore/history/azure-pipelines*

pool:
  vmImage: ubuntu-latest

steps:
- task: MavenAuthenticate@0
  displayName: 'Maven Authenticate'
  inputs:
    artifactsFeeds: mscore

- task: Maven@3
  displayName: 'Maven build'
  inputs:
    mavenPomFile: 'mscore/history/pom.xml'
    mavenOptions: '-Xmx3072m'
    javaHomeOption: 'JDKVersion'
    jdkVersionOption: '1.11'
    jdkArchitectureOption: 'x64'
    publishJUnitResults: true
    testResultsFiles: 'mscore/history/**/surefire-reports/TEST-*.xml'
    goals: 'install'
    options: -Drevision=$(Build.BuildNumber)
